
const names=require('./nameforthird')
const util=require('./funcforthird')
//const { dan } = require('./nameforthird')
//util.sayhi(dan)
util(names.dan)
//here ,util is not containing an object since sayhi function is not in the format of an object , but a direct relation
